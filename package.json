{
  "name": "4-documentacion",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docs:dev": "set NODE_OPTIONS=--openssl-legacy-provider && vuepress dev docs_",
    "docs:build": "set NODE_OPTIONS=--openssl-legacy-provider && vuepress build docs_"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vuepress": "^1.9.10"
  }
}
